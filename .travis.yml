sudo: false

language: node_js
node_js:
  - "10.8.0"

cache:
  yarn: true
  directories:
    - node_modules

before_install:
  - npm install -g yarn

jobs:
  include:
    - stage: build
      script: yarn build
